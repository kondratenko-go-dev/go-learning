1. -rwxrwxr-x 1 peekaboo peekaboo 2.3M Sep 10 14:58 app
2. Ни чего не вывела
3. Ни чего не вывела
4. func Println(a ...any) (n int, err error)
   Println formats using the default formats for its operands and writes to
   standard output. Spaces are always added between operands and a newline
   is appended. It returns the number of bytes written and any write error
   encountered.
5. Большой размер бинарника для простой программы объясняется тем, что компилятор по умолчанию 
   включает в него объёмный код стандартных библиотек и сопутствующую отладочную информацию.